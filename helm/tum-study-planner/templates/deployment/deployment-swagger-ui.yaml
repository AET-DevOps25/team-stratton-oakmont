# prettier-ignore
apiVersion: apps/v1
kind: Deployment
metadata:
  name: unified-swagger-ui
  namespace: {{ .Values.namespace }}
  labels:
    app: unified-swagger-ui
spec:
  replicas: 1
  selector:
    matchLabels:
      app: unified-swagger-ui
  template:
    metadata:
      labels:
        app: unified-swagger-ui
    spec:
      containers:
        - name: swagger-ui
          image: "{{ .Values.swaggerUi.image.repository }}:{{ .Values.swaggerUi.image.tag }}"
          ports:
            - containerPort: 80
          env:
            - name: NGINX_ENVSUBST_OUTPUT_DIR
              value: "/etc/nginx/conf.d"
          resources:
            requests:
              memory: "64Mi"
              cpu: "50m"
            limits:
              memory: "128Mi"
              cpu: "100m"
